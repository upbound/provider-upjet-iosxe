---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vtps.switching.iosxe.upbound.io
spec:
  group: switching.iosxe.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - iosxe
    kind: VTP
    listKind: VTPList
    plural: vtps
    singular: vtp
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VTP is the Schema for the VTPs API. This resource can manage
          the VTP configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VTPSpec defines the desired state of VTP
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deleteMode:
                    description: |-
                      is. Default value is all.
                      Configure behavior when deleting/destroying the resource. Either delete the entire object (YANG container) being managed, or only delete the individual resource attributes configured explicitly and leave everything else as-is. Default value is `all`.
                      - Choices: `all`, `attributes`
                    type: string
                  device:
                    description: |-
                      (String) A device name from the provider configuration.
                      A device name from the provider configuration.
                    type: string
                  domain:
                    description: |-
                      (String) Set the name of the VTP administrative domain.
                      Set the name of the VTP administrative domain.
                    type: string
                  file:
                    description: |-
                      (String) Configure IFS filesystem file where VTP configuration is stored.
                      Configure IFS filesystem file where VTP configuration is stored.
                    type: string
                  interface:
                    description: |-
                      (String) The name of the interface providing the VTP updater ID for this device
                      The name of the interface providing the VTP updater ID for this device
                    type: string
                  interfaceOnly:
                    description: |-
                      (Boolean) Use only this interface's IP address as the VTP IP updater address
                      Use only this interface's IP address as the VTP IP updater address
                    type: boolean
                  modeClient:
                    description: |-
                      (Boolean) Set the device to client mode
                      Set the device to client mode
                    type: boolean
                  modeClientMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeClientUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeClientVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeOff:
                    description: |-
                      (Boolean) Set the device to off mode
                      Set the device to off mode
                    type: boolean
                  modeOffMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeOffUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeOffVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeServer:
                    description: |-
                      (Boolean) Set the device to server mode
                      Set the device to server mode
                    type: boolean
                  modeServerMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeServerUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeServerVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeTransparent:
                    description: |-
                      (Boolean) Set the device to transparent mode
                      Set the device to transparent mode
                    type: boolean
                  modeTransparentMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeTransparentUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeTransparentVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  passwordHidden:
                    description: |-
                      (Boolean) Set the VTP password hidden option
                      Set the VTP password hidden option
                    type: boolean
                  passwordSecret:
                    description: |-
                      (Boolean, Sensitive) Specify the vtp password in encrypted form
                      Specify the vtp password in encrypted form
                    type: boolean
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The ascii password for the VTP administrative domain
                      The ascii password for the VTP administrative domain
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  passwordSecretWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordSecretWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  passwordWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  pruning:
                    description: |-
                      (Boolean) Set the administrative domain to permit pruning
                      Set the administrative domain to permit pruning
                    type: boolean
                  version:
                    description: |-
                      (Number) Set the administrative domain to VTP version
                      Set the administrative domain to VTP version
                      - Range: `1`-`3`
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteMode:
                    description: |-
                      is. Default value is all.
                      Configure behavior when deleting/destroying the resource. Either delete the entire object (YANG container) being managed, or only delete the individual resource attributes configured explicitly and leave everything else as-is. Default value is `all`.
                      - Choices: `all`, `attributes`
                    type: string
                  device:
                    description: |-
                      (String) A device name from the provider configuration.
                      A device name from the provider configuration.
                    type: string
                  domain:
                    description: |-
                      (String) Set the name of the VTP administrative domain.
                      Set the name of the VTP administrative domain.
                    type: string
                  file:
                    description: |-
                      (String) Configure IFS filesystem file where VTP configuration is stored.
                      Configure IFS filesystem file where VTP configuration is stored.
                    type: string
                  interface:
                    description: |-
                      (String) The name of the interface providing the VTP updater ID for this device
                      The name of the interface providing the VTP updater ID for this device
                    type: string
                  interfaceOnly:
                    description: |-
                      (Boolean) Use only this interface's IP address as the VTP IP updater address
                      Use only this interface's IP address as the VTP IP updater address
                    type: boolean
                  modeClient:
                    description: |-
                      (Boolean) Set the device to client mode
                      Set the device to client mode
                    type: boolean
                  modeClientMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeClientUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeClientVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeOff:
                    description: |-
                      (Boolean) Set the device to off mode
                      Set the device to off mode
                    type: boolean
                  modeOffMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeOffUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeOffVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeServer:
                    description: |-
                      (Boolean) Set the device to server mode
                      Set the device to server mode
                    type: boolean
                  modeServerMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeServerUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeServerVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeTransparent:
                    description: |-
                      (Boolean) Set the device to transparent mode
                      Set the device to transparent mode
                    type: boolean
                  modeTransparentMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeTransparentUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeTransparentVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  passwordHidden:
                    description: |-
                      (Boolean) Set the VTP password hidden option
                      Set the VTP password hidden option
                    type: boolean
                  passwordSecret:
                    description: |-
                      (Boolean, Sensitive) Specify the vtp password in encrypted form
                      Specify the vtp password in encrypted form
                    type: boolean
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The ascii password for the VTP administrative domain
                      The ascii password for the VTP administrative domain
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  passwordSecretWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordSecretWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  passwordWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  pruning:
                    description: |-
                      (Boolean) Set the administrative domain to permit pruning
                      Set the administrative domain to permit pruning
                    type: boolean
                  version:
                    description: |-
                      (Number) Set the administrative domain to VTP version
                      Set the administrative domain to VTP version
                      - Range: `1`-`3`
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: VTPStatus defines the observed state of VTP.
            properties:
              atProvider:
                properties:
                  deleteMode:
                    description: |-
                      is. Default value is all.
                      Configure behavior when deleting/destroying the resource. Either delete the entire object (YANG container) being managed, or only delete the individual resource attributes configured explicitly and leave everything else as-is. Default value is `all`.
                      - Choices: `all`, `attributes`
                    type: string
                  device:
                    description: |-
                      (String) A device name from the provider configuration.
                      A device name from the provider configuration.
                    type: string
                  domain:
                    description: |-
                      (String) Set the name of the VTP administrative domain.
                      Set the name of the VTP administrative domain.
                    type: string
                  file:
                    description: |-
                      (String) Configure IFS filesystem file where VTP configuration is stored.
                      Configure IFS filesystem file where VTP configuration is stored.
                    type: string
                  id:
                    description: (String) The path of the object.
                    type: string
                  interface:
                    description: |-
                      (String) The name of the interface providing the VTP updater ID for this device
                      The name of the interface providing the VTP updater ID for this device
                    type: string
                  interfaceOnly:
                    description: |-
                      (Boolean) Use only this interface's IP address as the VTP IP updater address
                      Use only this interface's IP address as the VTP IP updater address
                    type: boolean
                  modeClient:
                    description: |-
                      (Boolean) Set the device to client mode
                      Set the device to client mode
                    type: boolean
                  modeClientMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeClientUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeClientVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeOff:
                    description: |-
                      (Boolean) Set the device to off mode
                      Set the device to off mode
                    type: boolean
                  modeOffMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeOffUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeOffVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeServer:
                    description: |-
                      (Boolean) Set the device to server mode
                      Set the device to server mode
                    type: boolean
                  modeServerMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeServerUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeServerVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  modeTransparent:
                    description: |-
                      (Boolean) Set the device to transparent mode
                      Set the device to transparent mode
                    type: boolean
                  modeTransparentMst:
                    description: |-
                      (Boolean) Set the mode for MST VTP instance
                      Set the mode for MST VTP instance
                    type: boolean
                  modeTransparentUnknown:
                    description: |-
                      (Boolean) Set the mode for unknown VTP instances
                      Set the mode for unknown VTP instances
                    type: boolean
                  modeTransparentVlan:
                    description: |-
                      (Boolean) Set the mode for VLAN VTP instance
                      Set the mode for VLAN VTP instance
                    type: boolean
                  passwordHidden:
                    description: |-
                      (Boolean) Set the VTP password hidden option
                      Set the VTP password hidden option
                    type: boolean
                  passwordSecret:
                    description: |-
                      (Boolean, Sensitive) Specify the vtp password in encrypted form
                      Specify the vtp password in encrypted form
                    type: boolean
                  passwordSecretWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordSecretWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  passwordWo:
                    description: |-
                      only) The write-only value of the attribute.
                      The write-only value of the attribute.
                    type: string
                  passwordWoVersion:
                    description: |-
                      only version of the attribute.
                      The write-only version of the attribute.
                    type: number
                  pruning:
                    description: |-
                      (Boolean) Set the administrative domain to permit pruning
                      Set the administrative domain to permit pruning
                    type: boolean
                  version:
                    description: |-
                      (Number) Set the administrative domain to VTP version
                      Set the administrative domain to VTP version
                      - Range: `1`-`3`
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
