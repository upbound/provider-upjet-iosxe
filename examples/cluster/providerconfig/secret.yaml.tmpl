apiVersion: v1
kind: Secret
metadata:
  name: example-creds
  namespace: crossplane-system
type: Opaque
stringData:
  credentials: |
    {
      "username": "admin",
      "password": "t0ps3cr3t11",
      "host": "10.0.0.1:830",
      "insecure": true,
      "retries": 3
    }
    # A single ProviderConfig can also manage several devices that share these
    # credentials. Managed resources then pick one with
    # spec.forProvider.device, and resources without a device are reconciled
    # against the device configured in "host" above:
    #
    # "devices": [
    #   {"name": "leaf1", "host": "10.0.0.1:830"},
    #   {"name": "leaf2", "host": "10.0.0.2:830"}
    # ]
